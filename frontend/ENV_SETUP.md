# Frontend Environment Setup

Create a `.env` file in the `frontend` directory with:

```env
VITE_API_URL=http://localhost:5005
```

Vite will expose this as `import.meta.env.VITE_API_URL` in the React app.

